# URL base da API do backend
# NOTA: Em desenvolvimento local (npm run dev), o Vite usa proxy automático para /v1 -> localhost:8000
# Esta variável é usada apenas em PRODUÇÃO (npm run build)
# Para desenvolvimento: http://localhost:8000
# Para produção: https://backdovidal.dev/v1
VITE_API_URL=https://backdovidal.dev/v1
